/* Reset de estilos básicos */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

/* Estilo para o corpo da página */
body {
  font-family: Arial, sans-serif;
  background: linear-gradient(to right, lightgreen, white);
  margin: 0;
  padding: 20px;
}

/* Estilos para o cabeçalho */
#cabeçalho {
  text-align: center;
  margin-bottom: 20px;
}

.container {
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.container img {
  max-width: 100%;
  height: auto;
  margin-right: 20px;
}

/* Estilos para as colunas */
.colunas-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  margin-bottom: 20px;
}

.coluna {
  flex: 1;
  background-color: rgba(248, 242, 242, 0.39);
  padding: 10px;
  border-radius: 5px;
  text-align: justify;
  max-width: 400px; /* Limita a largura máxima das colunas */
}

/* Estilos para as imagens */
.imagem-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap;
  gap: 20px;
  margin-bottom: 20px;
}

.imagem-container img {
  max-width: 100%;
  height: auto;
  max-height: 300px; /* Limita a altura máxima das imagens */
}
